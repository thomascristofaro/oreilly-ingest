library component not so good, and convert se hai oeps folder
quando metti il cookies non refresh il check
quando parte il download non refresh la lista
rimuovi il log per HTTP/active
sistemare la percentuale, mettere anche li immagini nella percentuale